"""
phase6_pillar_b_voice — Complete Voice Agent (M3 Phase 5 integration).

Sub-packages:
  booking/   — Slot resolver, PII scrubber, booking codes, waitlist
  dialogue/  — FSM, intent router, compliance guard, session manager
  voice/     — STT engine, TTS engine, VAD, audio pipeline
  mcp/       — Calendar, Sheets, Email MCP tools + orchestrator
"""
