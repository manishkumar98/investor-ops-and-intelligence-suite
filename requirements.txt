# Core AI
anthropic==0.40.0
openai==1.54.3
groq==0.11.0

# Vector DB
chromadb==1.5.8

# LangChain (corpus loading + chunking)
langchain==0.3.28
langchain-community==0.3.31
langchain-openai==0.2.8

# RAG Embeddings (phase6 voice RAG injector)
sentence-transformers==3.2.1

# NLP / PII scrubbing
spacy==3.8.14
# after install: python -m spacy download en_core_web_sm

# Voice — STT
deepgram-sdk==3.7.2
google-cloud-speech==2.29.0

# Voice — TTS (phase6)
gTTS==2.5.3
pyttsx3==2.98

# Voice — audio I/O (Handled by browser via JS)

# Google APIs (Calendar, Sheets, Gmail — phase6 MCP tools)
google-api-python-client==2.152.0
google-auth==2.36.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# UI
streamlit==1.57.0

# Timezone (IST — used throughout voice agent)
pytz==2024.2

# Data
pandas==2.2.3

# Review scraping (M2 pipeline)
google-play-scraper==1.2.7
emoji==2.14.0

# Testing
pytest==8.3.3
pytest-mock==3.14.0
pytest-asyncio==0.24.0
pytest-json-report==1.5.0
pytest-cov==6.0.0

# Utilities
python-dotenv==1.0.1
httpx==0.27.2
tiktoken==0.8.0
requests==2.32.3
